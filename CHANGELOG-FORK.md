# Fork 维护说明 / CHANGELOG-FORK

本仓库是 [`openai/codex-plugin-cc`](https://github.com/openai/codex-plugin-cc) 的**私有自维护 fork**(`stable` 分支),用于在官方维护停滞期间合入**经过 Opus + Codex 双重安全审计**的稳定性修复(主要解决「卡死 / 无响应」)。

## 来源与合规(Apache-2.0)

- **Upstream**: `openai/codex-plugin-cc`
- **Fork 基线**: `807e03ac9d5aa23bc395fdec8c3767500a86b3cf`(v1.0.4,2026-04-18,已逐字节核验等于官方 main tip)
- **许可证**: Apache-2.0(保留原 `LICENSE` 与 `NOTICE`)
- 本 fork 的所有修改在下方 Changelog 标注

## 维护原则(安全不变量,不可破坏)

- **零运行时依赖**:绝不引入任何 runtime dependency
- **无对外网络**:核心代码不新增 `fetch/http/net.connect` 到外部主机
- **无 shell 注入**:`spawn` 一律用数组参数,不拼接 shell 字符串
- **不放宽 CI**:保持 `on: pull_request`,绝不改成 `pull_request_target`
- **双审强制**:每个合入的 upstream PR 必须通过 Opus + Codex 双重安全审计(Verdict 一致 PASS 才合)
- **干净落地**:审计 PR 的净 diff 后,重落为本 fork 的干净 commit(co-author 原作者),不直接 merge 陌生人分支历史
- **回归门禁**:合入后 `npm test` 必须在干净环境(`env -u CLAUDE_PLUGIN_DATA -u CODEX_COMPANION_SESSION_ID ...`)全绿

## Changelog

### [未发布] stable

**已合入**(均经 Opus + Codex 双重安全审计:9 条红线全清、0 安全发现):

- `docs`: 添加 fork 维护计划(`IMPLEMENTATION_PLAN.md`)与本说明 — `71e17e8`
- `security`: 修复 untracked 文件 symlink 逃逸导致的凭据泄露(`lib/git.mjs`,`realpathSync` 限定 workspace 内)— `487b54d`,边界测试/TOCTOU 标注 `138958e`。Opus 设计+验证,Codex 独立复审 NEEDS_DISCUSSION(**无安全阻断**;TOCTOU 残余需本机并发执行能力、在威胁模型外,已诚实标注)
- #300 `fix`: broker shutdown 加 wall-clock timeout,broker 接受连接却不回复时不再无限挂 — `357893b`
- #302 `feat`: JSON-RPC `request()` 加 per-request wall-clock timeout + turn idle timeout,根治「卡死/无响应」主根因(request 永久 pending)— `18d3ee9`
- #361 `feat`: 加固 Codex turn 生命周期 — 用 `Promise.race([work, ceiling, stall, exit])` + `interruptTurnBestEffort` 替换 #302 在 captureTurn 的 idle 机制(额外覆盖 pre-ACK start RPC hang/reject、abandoned turn 上游 interrupt 防重叠重试),保留 #302 的 app-server `request()` 超时;broker 闲置自杀(`CODEX_COMPANION_BROKER_IDLE_MS` 默认 30min)+ `onSocketGone` 孤儿 turn 自毁(根治 broker 进程泄漏)+ shutdown 先关 listener 再关 appClient;失败回传错误而非空响应(治「无响应」)— `8be4ab3`。**双审 Opus + Codex(gpt-5.5 xhigh)均 APPROVE**,9 红线全清、0 安全发现,3 个互相印证的 LOW 非阻断(onSocketGone `error+close` 双事件幂等但无专测、`CEILING_MS` 兜底路径无专测、#355 后台任务交互——#355 未合,非缺陷)。全套 98/98 干净环境绿
- `fix`(外部 review 跟进):加固 `connect()` 失败清理 + 统一 timeout env 解析 — `9ae3ce1`。**P2**:`connect()` 在 `initialize()` 失败时不再遗弃已 spawn 的 app-server / broker socket;清理做成**有界且健壮**——`Promise.race([close(), 2s cap])` 防无界挂、`close()` 把 SIGTERM 升级到 SIGKILL(守卫 `exitCode===null && signalCode===null`)、再 destroy 父侧 stdout/stderr 读流(切断后代继承 stdio fd 对父进程事件循环的牵制)。**P3**:turn/stall/broker-idle 的 `Number(x)||default` 统一为 `resolveTimeoutMs(env, default, {allowDisable})`——拒非有限/负数、上界 clamp 到 2³¹-1(防超大值被 Node 截成 1ms 立即触发)、保留 RPC 的 0=禁用语义。**双审 Opus + Codex(gpt-5.5 xhigh)历经 4 轮**:Codex 在 P2 第一版连抓 **3 个实测可复现的真 HIGH**(无界 close 挂 / 未 reap 子进程 stdio 句柄卡父进程 / 孙进程继承 fd 卡父进程),逐一修复且每个都配「拆掉即失败」的回归测试;两边终审均 **APPROVE**,9 红线全清。全套 105/105 干净环境绿。**教训:单审挡不住这类底层(Node 子进程 stdio 继承)缺陷,跨 LLM 多轮对抗是必要的**
- `fix`(push 前最终独立交叉终审):broker `shutdown()` 加有界 drain — `61a3c52`。终审用**全新无作者上下文的独立 Opus 审计员 + fresh Codex** 冷审完整 delta(`c7d071a..HEAD`)。Codex 抓到 `onSocketGone`/idle 自毁里 `await server.close()` **要等所有连接结束才回调**——若第二个 broker 客户端(同 cwd 并发会话)半开/卡住不回 FIN,`server.close()` 永不完成 → 永不关 upstream、永不退出,#361 的孤儿 turn 自毁失效(**HIGH**,我和上一轮双审都漏了、独立冷审才逮到)。修:`server.close()` 与 `SHUTDOWN_DRAIN_MS`(1s)定时器 race,到期 destroy 半开连接,shutdown 始终有界;idle 路径也对齐 `onSocketGone` 的 `.finally(()=>process.exit(0))`(防 shutdown reject 时漏退出)。补「半开 straggler + broker/shutdown → broker 仍有界退出」回归测试(拆掉即 8s 挂死)。**独立 Opus APPROVE(0 Critical/Important)、Codex REQUEST CHANGES(此 HIGH)——分歧按铁律保守裁决、主线显式裁定修复后重审**。全套 106/106 干净环境绿
- `fix`(最终独立重审采纳的两条加固):`close()` 双侧加固 — `81bdb9a`。重审后两个全新独立审计员**均 APPROVE**,各提一条非阻断、同属「卡死/泄漏」类的 `close()` 健壮性建议,主线采纳:① **SpawnedClient.close()** 外层 kill 门从 `!this.proc.killed` 改为 `exitCode===null && signalCode===null`(Node 在信号_发送_后即置 killed,旧门在 close() 前已发过 SIGTERM 的潜伏场景会连 SIGKILL 升级一起跳过 → 重新卡死;当前不可触发但对齐注释、防未来调用方)(独立 Opus Finding 1);② **BrokerClient.close()** 的 socket 改 `end()` + `destroy()`(只 end 是半关,rogue/卡住的本地 endpoint 接受连接却不回 FIN 时会留 ref'd socket + exitPromise 不 resolve、卡住宿主进程)(独立 Codex MEDIUM),补「rogue endpoint → 宿主仍自然退出」回归测试(拆掉即 15s 挂死)。全套 107/107 干净环境绿
- `fix`(2026-06-11 排查「后台 agent 无声消失 + `--cd` 不生效」,版本 100.0.2):turn watchdog 阈值/判定 + task cwd 路由 — `6157d0d`+`3be9e75`+`659561b`+`07c08bd`+`4d4a318`(5 commits)。**问题 1**:per-turn watchdog 默认值对真实 xhigh 任务太紧(实测 28min 实现任务距 30min ceiling 仅 2min)→ CEILING 30→90min、STALL 10→20min;并新增 `inFlightItems` 追踪——未完成 item / 活跃 subagent turn / 待定 collaboration 期间纯 inbound 静默**不再判 stall**(真挂死由无条件、不可禁用的 ceiling 兜底)。**问题 2 真根因**(比排查报告的猜测更具体):`--cd` 不是 companion 旗标,被 `parseArgs` 静默吞进 prompt 文本 → codex 跑在调用方 cwd(主 checkout)甚至照 prompt 文本自行 /tmp clone → 修:`--cd`→`--cwd` 别名、启动类命令(task/review/adversarial-review)严格校验目录存在(job-control 命令保持宽松:state 按路径 hash 存于 workspace 外,worktree 删除后 status/result/cancel 必须仍可达,有专测)、task 输出回显 workspace root、agent/skill 文档补 `-C` 路由契约与 `--` passthrough 逃生门。**附带根治 detached worker「幽灵 queued」**:绝对 `--cwd` 改 `path.resolve(given)` 不触 `process.cwd()`(新 worker 冷缓存首次调用对已删 cwd 抛 `uv_cwd ENOENT` 于状态机外)+ worker 从 `os.tmpdir()` spawn(spawn cwd 不可能被 worktree 清理删除)+ 尾斜杠不再分裂 state-dir key。**五轮跨 LLM 对抗审计**:Codex(gpt-5.5 xhigh,同线程贯穿)NEEDS DISCUSSION→REQUEST CHANGES→REQUEST CHANGES→APPROVE→APPROVE,连抓 3 个递进真问题(cwd 校验范围过宽 / worker `process.cwd()` 幽灵 / `path.normalize` 尾斜杠 state-key 偏移);独立 Fable 审计员(每轮全新无作者上下文+隔离 worktree)APPROVE→APPROVE→REQUEST CHANGES→最终冷审 APPROVE,其 REQUEST CHANGES 抓到**幽灵回归测试因 Node JS 层 cwd 缓存在所有平台空转**(`rmSync(process.cwd())` 这步本身焐热缓存;须先 `chdir` 失效化才咬合——修后实测 pre-fix 3 连败/fixed 3 连过)并纠正机制叙事(「stale 路径」是 Node 跨平台缓存行为而非 macOS 差异)。9 红线全清,全套 **114/114** 干净环境绿(107+7 个「拆掉即失败」回归测试)。**#355 调查结论**:broker idle 收割仅在无 client 连接时触发(有 socket 即重置),不杀活跃后台 job;SessionEnd 清理本会话 job 属设计行为;`TaskStop`/`TaskOutput` 注册表不一致属 Claude Code harness 侧,本仓无法修——维持 HOLD 不合入。注:本批前两个 commit 经历过一次本地 checkout 意外删除,自已审 diff 逐字重建(原审 SHA `87bd5e0`/`accab1f` → 重建 `6157d0d`/`3be9e75`,diff stats 逐项核对一致;最终冷审对重建树做了整体连贯性检查)

- `feat`+`fix`(2026-07-04 批,版本 100.0.4):移植上游 v1.0.5(`/codex-stable:transfer`)+ 修「subagent 跨 workspace 后台 job 对主会话不可见」 — `6cd6b8d`+`7740159`+`dddeaaa`(3 commits)。**移植**(净 diff `807e03a..80c31f9` = #374+#398,co-author 原作者):/transfer 把 `~/.claude/projects` 下的 Claude 会话 JSONL 经 app-server `externalAgentConfig/import` 导入为可 resume 的 Codex 线程(SessionStart hook 新导出 `CODEX_COMPANION_TRANSCRIPT_PATH`;路径校验 realpath+containment+`.jsonl`);app-server 退出错误附 stderr;capabilities 加 `requestAttestation:false`;`buildThreadParams` 去 `experimentalRawEvents`。**Codex(gpt-5.5 xhigh)对上游原版判 REQUEST CHANGES(2 MEDIUM + 1 LOW),按保守裁决在移植时加固**:① `request()` 支持按次**有界** `timeoutMs` 覆盖,import 请求用与 completed 等待相同的 2min 上界(否则本 fork #302 的 30s RPC 超时会截断大 transcript 导入;上游原版则在 request 永久 pending 时挂死且跳过 client close);② `importedThreadIdForSource` 在任何读取/哈希**之前**复核 canonical path 未被换(封台账侧 TOCTOU);③ transfer 的 `--cwd` requireExists(fork 的启动类命令惯例)。LOW(transcript 在 codex 读取与台账查找之间被追加 → 误报)保留上游语义,见已知限制。**Bug 修复**(用户实测:codex-rescue subagent 用 `-C <worktree>` 起的后台 job,主会话 `/codex-stable:status` 显示 "direct startup"+空列表,而 worker/broker 都在 worktree 上正常干活):根因**不是** sessionId 缺失(`CODEX_COMPANION_SESSION_ID` 在 subagent 环境即主会话 id,记录一直打得对),而是 **job 状态按 workspace root 哈希分目录**、主会话只读自己那份。修:读侧聚合——有 session id 时 `listSessionJobGroups` 扫描 state root 下全部 workspace state 目录,按 sessionId 合并进 status / 单 job 查询(含 `--wait`)/ result / cancel;result/cancel 用 matched job 自身 `workspaceRoot` 读写;cancel 的 turn/interrupt 走 job 的 workspace;SessionEnd 跨 workspace 清扫(worktree job 不再泄漏出会话);render 对非当前 workspace 的 job 标注 `Workspace:`;无 `workspaceRoot` 的记录跳过(后续命令反正找不到其文件);resume-last **有意**保持 workspace 内(跨目录续线程会把新 turn 路由到错误目录);无 session id 时行为完全不变。已在真实 state root 实测:修复版可见跨 workspace job,已安装 100.0.3 不可见(复现原症状)。**终审双 APPROVE**:Codex(gpt-5.5 xhigh)APPROVE(1 LOW:扫描端非对象 job 记录会炸整个扫描、SessionEnd 连带跳过 broker teardown——`dddeaaa` 已修 + 有牙测试)+ 独立无作者上下文 Fable 冷审(隔离 worktree)APPROVE(0 Critical/Important、4 Minor:#2 台账/源文件裸错 `dddeaaa` 已修;#1/#4 记已知限制;#3「`removeSessionJobsFromStateDir` 不走 saveState 的 prune」裁定为有意且更安全——只删本会话记录,不会因 MAX_JOBS 误删同目录他会话溢出文件)。冷审含拆桩验证(短路 `listOtherWorkspaceSessionJobs` → 2 个跨 workspace 测试立红)。既有测试凡向 job 命令传 session id 的一律改 **per-run 唯一 id**(共享 fallback state root 下固定 id 会串起历史运行残留、SessionEnd 会 signal 其记录的 stale pid)。全套 **123/123** 干净环境绿(114 → 123:+4 transfer、+1 corrupt-ledger、+3 跨 workspace、+1 corrupt-scan)

- `security`(2026-07-08 批,版本 100.0.5):移植上游 v1.0.6(#447 "Remove shell expansion for git commands",净 diff `80c31f9..db52e28`,co-author bryane-oai)— `b11d465`。**Windows git 命令注入修复 = HOLD 清单 #294 的官方修法**:此前 `runCommand` 在 win32 默认 `shell: process.env.SHELL || true`,`git.mjs` 的 `git()`/`gitChecked()` 携带仓库派生参数(symbolic-ref 解析的 baseRef / 分支名 / 文件路径)经 cmd.exe 或用户 SHELL 展开——恶意仓库把默认分支命名为 `main&evil` 之类即可在 review 时注入任意命令。修:`git()`/`gitChecked()` 强制 `shell: false` **且置于 options spread 之后**(调用方不可能意外覆盖回 shell);`runCommand` / `tests/helpers.run` 改 `options.shell ?? 平台默认`(`??` 尊重显式 false,未传时行为不变);新增回归测试:分支名 `main&branch-helper&x` 必须字面到达 git、repo 内 `branch-helper.cmd` 不被执行(Windows 上拆掉修复即失败;POSIX 上 shell 恒 false、`&` 为合法 refname 字符,稳定)。macOS/POSIX 运行时**零行为变化**。落地:3 个代码/测试文件补丁与上游逐 hunk 一致(仅 blob index 因 fork 基线不同);`helpers.mjs` 与 fork 的 `timeout: options.timeout` 行语义合并;版本 hunks 走 fork 自己的 100.0.5;fork 的 symlink 防护(`formatUntrackedFile`)与本补丁互不冲突、同时保留。**双审 Claude(Fable 5 主线)+ Codex(gpt-5.5 xhigh,fresh 只读,固定 SHA 静态审计)均 APPROVE**,供应链红线全清;Codex 1 MEDIUM(非 git 的 win32 shell 面仍存——既存面、非本批引入,见已知限制)+ 2 LOW(git 仅以 `.cmd`/`.bat` shim 暴露的罕见 Windows 环境会 spawn 失败,响亮报错、兼容性边界非安全倒退;合并须保留 fork 既有改动——已照做)。全套 **124/124** 干净环境绿(123 + 1 注入回归)

- `chore`(2026-07-10,版本 100.0.6):版本号对齐上游——零代码改动,仅四个 manifest 的版本字段 100.0.5 → 100.0.6(`scripts/bump-version.mjs`)。**此后版本策略:移植完上游 `v1.0.N` 后,fork 版本即对齐为 `100.0.N`**(此前 100.0.5 对应上游 v1.0.6,错位一格易记混);两次上游发布之间 fork 自有批次若需发版,递增第三段并在本文件标注实际对应的上游版本,待下次移植重新对齐。

**暂缓 / 不合入**:

- #312(per-turn watchdog):**已被 #361 取代(SUPERSEDED),永久弃用**。#312 的 watchdog 打不断卡住的 `startRequest()`,timer reject 一个无人 await 的 promise → unhandledRejection / exit 1(而非 PR 承诺的 exit 124)——Codex 在 Node v24.10.0 复现 HIGH 正确性缺陷;新测试也只覆盖错误对象形状、未测真实 timeout 行为。#361 用 `Promise.race`(从创建即观测所有 racer、包裹 start RPC、上游 interrupt)**正确实现了 #312 想做的事**,已于 `8be4ab3` 合入,故 #312 不再合入。
- #294(Windows baseRef 命令注入的社区修法):**已被上游 v1.0.6(#447)取代(SUPERSEDED)**——其要解决的根因(仓库派生参数经 win32 shell 展开)已由 `shell: false` 在源头正确修复(2026-07-08 批 `b11d465` 合入),且没有 #294 自带的 `cmd.exe` 校验注入风险,不再合入。
- #343 / #355:仍 **HOLD**(未变)。#343 PowerShell 进程清理自带注入 + 误杀风险(其针对的 taskkill/进程清理路径未被 #447 触及);#355 background job broker 保活属独立功能(非缺陷),#361 的 broker 闲置自杀与其**互补**(一个保活一个收尸),如未来重度依赖跨会话后台任务再单独评估合入。
- #346(codex-rescue reliability pass):**HOLD,暂不考虑合并**(用户 2026-06-09 决定:改动太大)。16 文件 / 3099 行,且是整批唯一引入**自动执行 PostToolUse hook** 的 PR(`codex-rescue-completion-hook.mjs`:Agent 工具用后注入"已完成/别空等"上下文)+ 后台 task-worker spawn + 测试用 `NODE_OPTIONS` preload。功能为 codex-rescue 完成信号 + worker 生命周期检测,属**可靠性打磨**而非根因修复——「卡死/无响应」主根因已被 #302+#361 覆盖,边际价值低;risk/reward 全批最差(最大改动 + 新增 auto-exec hook 攻击面)。快扫未见明显危险(hook 仅读 state 吐文本、spawn 用数组参数),但**未做 merge 级完整双审**。将来若"Claude 在 codex-rescue 已返回后仍空等/轮询"症状明显,再正经双审或只 cherry-pick 最小完成信号版。

**已知限制 / 未来加固候选**:

- **win32 上非 git 的 spawn 仍走 shell 默认**(Codex 2026-07-08 终审 MEDIUM;既存面、非本批引入,与上游 v1.0.6 一致):`binaryAvailable`(codex/node/npm,固定参数)、`SpawnedCodexAppServerClient` 启动 `codex app-server`、`terminateProcessTree` 的 taskkill(PID 已 `Number.isFinite` 限制)在 win32 仍经 shell 启动。无仓库派生参数拼入 shell,注入面不变;残余风险是**不可信 repo 作为 cwd 时的命令查找 / 本地 shim 劫持**(cmd.exe 的可执行解析可能命中 cwd 下同名文件)。未来加固方向:这些调用点显式 `shell: false`(codex.exe/node.exe 可直接 spawn)或把 spawn cwd 挪出不可信 repo——属独立行为改动,需自己一轮双审,不搭本批便车。

- **transfer 的台账 hash 匹配对「活文件」敏感**(上游语义,有意保留):Claude transcript 在 codex 读取与 companion 台账查找之间被追加 → `content_sha256` 不匹配 → 误报 "did not record an imported thread"(退出非 0,可重试)。不做「按路径回退取最近记录」——那会在真失败时静默 resume 到**旧**导入线程,比响亮误报更糟。
- **cancel 的优雅 turn/interrupt 在主会话自身持有 broker 时可能路由到主会话 broker**(2026-07-04 冷审 Minor #1;非回归,旧代码同病):`connect()` 端点优先级 env > `loadBrokerSession(cwd)`,job workspace 的 broker 拿不到 interrupt → `interrupted:false`。影响仅「上游优雅中断」;worker 仍被 pid 硬杀,broker 侧孤儿 turn 由 #361 的 onSocketGone 自毁兜底。加固方向:`interruptAppServerTurn` 显式取 `loadBrokerSession(workspaceRoot)?.endpoint` 传 `connect` 的 `brokerEndpoint`(需配双 broker 场景的回归测试,本批未扩)。
- **非 git workspace + 路径含 symlink + 目录被删**时 `resolveStateDir` 的 realpath 回退会算出不同 hash → 跨 workspace 读写错位(2026-07-04 冷审 Minor #4;既存属性,非本批引入):git worktree 场景(本修复的实际场景)被 `git rev-parse --show-toplevel` 恒返 canonical 路径中和,实测无影响。
- **review context 仍会读入 workspace _内_ 的敏感 untracked 文件**:`487b54d` 的 symlink 修复只挡住了**逃逸到 workspace 外**;workspace 内的 untracked `.env` / `.git/config`(无论是否经 symlink)仍会进入发往 Codex 的 review context(外部 reviewer 第 3 条意见,既存问题、非本批引入)。故本 fork 的 symlink 修复**不应被理解为「防住一切凭据读入 review」**。未来加固方向:构建 untracked review context 时跳过 gitignored / 已知敏感模式(`.env`、`.git/` 等)文件。
- **`close()` 的 SIGKILL 只杀直接 app-server 子进程、不 reap 其后代**:若真实 codex app-server spawn 了继承其 stdio 的工具/MCP 子进程,SIGKILL 父进程后这些**孙进程可能继续存活**(占资源 / 改 workspace)。**既存问题、非本批引入**(close 一直只杀直接子);P2 的 stream-destroy 已解决「后代继承 fd 卡住父进程退出」(**卡死**),但未解决「后代继续运行」(**孤儿**)。Codex 终审 MEDIUM、独立 Opus 未判为问题。未来加固:POSIX 进程组强杀(`detached` spawn + `kill -pid`)——是独立的 spawn 行为改动,需自己一轮审计,不在本次 push 前扩大范围。
- **hung in-flight item 的检测从 20min stall 退化到 90min ceiling**(2026-06-11 批,有意取舍):只 started 不 completed 的 item(含挂死 MCP 调用、或 started/completed id 不对称的 item 形态)会让该 turn 的 stall 抑制常开,wedge 要等 ceiling 才被放弃。ceiling 无条件、`resolveTimeoutMs` 拒 0/负值不可禁用,无无界路径;外层调度方的 watchdog 通常远早于 ceiling 介入。若未来要收紧:对「有 in-flight 但零流量」设 N×STALL 上限,或 `error` 通知时清 in-flight 集合(`error` 是否恒为终态未实证,故本批未做)。
- **`--cwd` 指向 workspace 子目录/symlink 别名 + 目录在 worker 读 state 前被删**的极窄窗口(毫秒级)仍可留下 stale queued 记录(最终冷审 LOW):创建时 state key 经 git toplevel+realpath 归一,目录删除后回退字面字符串,子目录字符串 hash ≠ toplevel hash。加固方向:`enqueueBackgroundTask` 给 worker 传 `job.workspaceRoot`(创建时的 canonical root)而非原始 cwd,可整类关死。顺带:`handleTaskWorker` 局部变量 `cwd` 未使用(既存),下次触碰该文件时一并清理。
- **task 文本中未加引号、未用 `--` 隔离的字面量 `-C`/`--cwd`/`--cd` token 会被当旗标消费**:`--cd` 为本批新增拼写(`-C`/`--cwd` 既存同类危险);后随 token 非存在目录 → 响亮报错,是存在目录 → 静默改道(`Workspace root:` 回显可见)。两份文档已约定:路由旗标在前、需要字面 token 时用 `--` passthrough。
